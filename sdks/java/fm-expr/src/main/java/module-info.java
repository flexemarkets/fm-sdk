/**
 * Panel expressions: the closed language a manager writes a participant
 * panel in, and its one evaluator. See {@link fm.expr.Expression} for the
 * grammar and {@link fm.expr.Panel} for how a panel's fields compose.
 */
module fm.expr {
    exports fm.expr;
}
