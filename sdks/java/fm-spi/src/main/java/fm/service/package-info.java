/**
 * <h2 style="color:#b00">&#9888; ALPHA SOFTWARE &mdash; THE API IS NOT STABLE</h2>
 *
 * <p><strong>Pre-1.0 and under active development.</strong> Anything public
 * here may be <strong>added, renamed or removed in any release</strong>,
 * breaking code that compiles and runs today, with no deprecation period
 * first. A {@code 0.x} version carries no compatibility promise under
 * semantic versioning, and these versions are used that way: treat every
 * bump as potentially breaking.
 *
 * <p>Pin an exact version rather than a range, read the upgrade notes for
 * the release you are moving to, and expect to change code when you move.
 * <strong>Not recommended for production use.</strong>
 *
 * <p>The plugin contract between a service and its host.
 */
package fm.service;
