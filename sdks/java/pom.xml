<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.flexemarkets</groupId>
    <artifactId>fm-sdk-parent</artifactId>
    <version>0.0.13-SNAPSHOT</version><!-- fm-version -->
    <packaging>pom</packaging>

    <name>Flexemarkets SDK Parent</name>
    <description>Parent POM for Flexemarkets SDK modules</description>
    <url child.project.url.inherit.append.path="false">https://github.com/flexemarkets/fm-sdk</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Flexemarkets</name>
            <email>support@flexemarkets.com</email>
            <organization>Flexemarkets</organization>
            <organizationUrl>https://flexemarkets.com</organizationUrl>
        </developer>
    </developers>

    <scm child.scm.connection.inherit.append.path="false"
         child.scm.developerConnection.inherit.append.path="false"
         child.scm.url.inherit.append.path="false">
        <connection>scm:git:git://github.com/flexemarkets/fm-sdk.git</connection>
        <developerConnection>scm:git:ssh://github.com:flexemarkets/fm-sdk.git</developerConnection>
        <url>https://github.com/flexemarkets/fm-sdk/tree/main</url>
    </scm>

    <modules>
        <module>fm-spi</module>
        <module>fm-sdk</module>
        <module>examples/ticker</module>
    </modules>

    <properties>
        <!--
            The lowest LTS the SDK can be built for, and deliberately not the
            newest. This is a published artifact: its class-file version is the
            floor for everyone who consumes it, so it should ask for as little
            as the code actually needs.

            21 is what the code needs. Thread.startVirtualThread (Events,
            DefaultMarketView) and HttpClient.close (HttpFlexemarkets) are
            Java 21 APIs, and Jackson 3 requires 17, so 17 was never reachable
            and 11 less so. Raising this floor should be a decision, not a
            side effect of a newer JDK being installed.
        -->
        <java.version>21</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jackson.version>3.2.1</jackson.version>
        <lanterna.version>3.1.3</lanterna.version>
        <!-- Don't publish by default: the parent POM and the ticker example
             stay off Central. Publishable library modules (fm-sdk, fm-spi)
             override this to false. -->
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.flexemarkets</groupId>
                <artifactId>fm-sdk</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!--
                Jackson 3. The BOM governs every Jackson artifact, matching how
                fm-server and fm-robots manage it, so an SDK consumer sharing a
                classpath with either does not end up straddling two majors.

                jackson-datatype-jsr310 is gone on purpose: Jackson 3 folded
                java.time support into databind, so registering JavaTimeModule
                is no longer needed and the separate artifact no longer exists.
            -->
            <dependency>
                <groupId>tools.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>com.googlecode.lanterna</groupId>
                <artifactId>lanterna</artifactId>
                <version>${lanterna.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <distributionManagement>
        <snapshotRepository>
            <id>central</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
        </snapshotRepository>
        <repository>
            <id>central</id>
            <url>https://central.sonatype.com/repository/maven-releases/</url>
        </repository>
    </distributionManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Flatten publishable modules to self-contained POMs (no
                     <parent>) so fm-sdk-parent need not be published. oss mode
                     inlines the Central-required metadata (licenses, scm,
                     developers, url) and resolves inherited dependency
                     versions. Activated per-module in fm-sdk and fm-spi. -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>flatten-maven-plugin</artifactId>
                    <version>1.7.3</version>
                    <configuration>
                        <flattenMode>oss</flattenMode>
                    </configuration>
                    <executions>
                        <execution>
                            <id>flatten</id>
                            <phase>process-resources</phase>
                            <goals>
                                <goal>flatten</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>flatten-clean</id>
                            <phase>clean</phase>
                            <goals>
                                <goal>clean</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.11.2</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <release>${java.version}</release>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <gpgArguments>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <!--
                            0.11.0, up from 0.7.0. The older client deserialised
                            the portal's status response into a fixed set of
                            fields, and the portal has since added `warnings`, so
                            waitUntil=published failed on every poll with an
                            UnrecognizedPropertyException.

                            That failure mode is the dangerous one: the bundle
                            uploads, the portal publishes it, and the build goes
                            red anyway. fm-spi 0.0.10 went out exactly that way
                            and looked like a failed release for as long as it
                            took to check repo1. Every registry here is
                            append-only, so the obvious response (run it again)
                            cannot work, and the second run reports the version
                            as already taken, which reads like the first one
                            having succeeded in some other sense.
                        -->
                        <version>0.11.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                            <!-- Wait for PUBLISHED, not the default VALIDATED.
                                 A validated bundle has passed its checks and
                                 has not been published: with autoPublish the
                                 portal goes on to publish it after the build
                                 has exited, so a failure there lands nowhere a
                                 person is looking. The whole point of the
                                 publish gate is that a release fails loudly
                                 while someone is still watching it, and every
                                 registry here is append-only: a half-landed
                                 version cannot be replaced, only renumbered.

                                 This does NOT mean the artifact is on
                                 repo1.maven.org: the Central sync runs
                                 afterwards and takes minutes to hours. It
                                 means Central accepted and published it, which
                                 is the part the build can be held to. -->
                            <waitUntil>PUBLISHED</waitUntil>
                            <!-- This plugin bundles every reactor module and
                                 does NOT honour maven.deploy.skip (that flag
                                 only governs maven-deploy-plugin, a different
                                 code path). excludeArtifacts is matched against
                                 artifactId and is the real knob for keeping the
                                 parent POM and the ticker example off Central.
                                 fm-sdk/fm-spi are flattened (parentless), so
                                 dropping fm-sdk-parent leaves nothing dangling. -->
                            <excludeArtifacts>
                                <excludeArtifact>fm-sdk-parent</excludeArtifact>
                                <excludeArtifact>fm-ticker</excludeArtifact>
                                <!-- fm-spi is on its own version line and has
                                     not moved since 0.0.11, which is already on
                                     Central. Left in, the bundle carries an
                                     already-published coordinate and Central
                                     refuses the whole thing, fm-sdk included.
                                     Remove this the next time the contract
                                     changes and set-spi-version moves it. -->
                                <excludeArtifact>fm-spi</excludeArtifact>
                            </excludeArtifacts>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
